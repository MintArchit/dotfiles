# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# set_group
# Mod4=Super Key
set $mod Mod4
set $refresh_i3status killall -SIGUSR1 i3status

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $wsa1 "1:1"
set $wsa2 "2:1"
set $wsa3 "3:1"
set $wsa4 "4:1"
#set $wsa5 "5:5"
#set $wsa6 "6:6"
#set $wsa7 "7:7"
#set $wsa8 "8:8"
#set $wsa9 "9:9"
#set $wsa0 "0:0"


#set $wsb1 "21:0"
#set $wsb2 "22:9"
#set $wsb3 "23:III"
#set $wsb4 "24:4"
set $wsb5 "5:2"
set $wsb6 "6:2"
set $wsb7 "7:2"
set $wsb8 "8:2"
#set $wsb9 "29:9"
#set $wsb0 "20:0"


set $monitor eDP-1
set $monitor2 HDMI-1
# End_set

# preference_group
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font_size=0 workaround to remove titlebar in stacked and & layout
# https://faq.i3wm.org/question/4224/reduce-space-used-by-stacking-containers-titlebars/index.html
font pango:monospace 0

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8


# You can also use any non-zero value if you'd like to have a border
default_border none
#default_border pixel 1
#default_floating_border pixel 1
#for_window [class=".*"] border pixel 1


workspace $wsa1 output monitor $monitor
workspace $wsa2 output monitor $monitor
workspace $wsa3 output monitor $monitor
workspace $wsa4 output monitor $monitor
workspace $wsa5 output monitor $monitor
workspace $wsa6 output monitor $monitor
workspace $wsa7 output monitor $monitor
workspace $wsa8 output monitor $monitor
workspace $wsa9 output monitor $monitor
workspace $wsa0 output monitor $monitor

workspace $wsb1 output monitor $monitor2
workspace $wsb2 output monitor $monitor2
workspace $wsb3 output monitor $monitor2
workspace $wsb4 output monitor $monitor2
workspace $wsb5 output monitor $monitor2
workspace $wsb6 output monitor $monitor2
workspace $wsb7 output monitor $monitor2
workspace $wsb8 output monitor $monitor2
workspace $wsb9 output monitor $monitor2
workspace $wsb0 output monitor $monitor2


gaps inner 6
gaps outer 0

# End_preference

# colors_group
# class                 border  backgr. text    indicator child_border
#client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.focused          #ff000000 #880066ff #000000 #2e9ef4   #285577
client.unfocused        #ff000000 #8800665a #000000 #292d2e   #222222
#client.urgent           #2f343a #900000 #ffffff #900000   #900000
#client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

#client.background       #ffffff
# End_colors

# exec_group
# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3
# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.
# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet
#exec --no-startup-id picom -f --xrender-fence
exec --no-startup-id picom -
exec --no-startup-id flameshot -t true
# End_exec

# exec_always_group
exec_always --no-startup-id ~/.fehbg -bg-fill &
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always --no-startup-id xrandr --output HDMI-1 --auto --above eDP-1
# switch keyboard layout
exec_always "setxkbmap -model pc105 -option 'grp:win_shift_toggle' -layout us,de -variant ,qwerty"
# setxkbmap -query
#setxkbmap -layout de,en
#setxkbmap -option 'grp:ctrl_alt_toggle'
#bindsym $mod+j exec --no-startup=id setxkbmap de
#bindsym $mod+k exec --no-startup-id setxkbmap us
# End_exec_always

# bindsym_group
# Use pactl to adjust volume in PulseAudio.
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# program hotkeys
bindsym $mod+t exec i3-sensible-terminal # start a terminal
bindsym $mod+b exec firefox
bindsym $mod+e exec nemo

# kill focused window
bindsym $mod+F4 kill

# start dmenu/rofi (a program launcher)
bindsym $mod+F2 exec --no-startup-id dmenu_run -b
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
bindsym $mod+a exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus vi direction
#bindsym $mod+j focus left
#bindsym $mod+k focus down
#bindsym $mod+l focus up
#bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window vi directions
#bindsym $mod+Shift+j move left
#bindsym $mod+Shift+k move down
#bindsym $mod+Shift+l move up
#bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+backslash split h

# split in vertical orientation
bindsym $mod+minus split v

# enter fullscreen mode for the focused container
bindsym $mod+F11 fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
#bindsym $mod+e layout toggle split
#bindsym $mod+Alt+Left layout toggle splith tabbed stacked splitv
bindsym $mod+Tab layout toggle tabbed splith splitv 
bindsym $mod+Shift+Tab layout toggle splitv splith tabbed

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+x focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 exec --no-startup-id i3-msg workspace 1:1
bindsym $mod+2 exec --no-startup-id i3-msg workspace 2:1


bindsym $mod+Ctrl+1 exec --no-startup-id i3-msg workspace 5:2
bindsym $mod+Alt+2 exec --no-startup-id i3-msg workspace 6:2

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Ctrl+Shift+r exec "i3 reload && i3 restart"

# powermenu hotkeys
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+s exec poweroff
bindsym $mod+Shift+r exec reboot
bindsym $mod+Shift+e exec "i3-msg exit"
bindsym $mod+Shift+l exec $HOME/ghub/scripts/utils/lock.sh
bindsym $mod+comma exec gnome-control-center
bindsym Print exec "flameshot full"
bindsym Shift+Print exec "flameshot gui"

# resize window (you can also use the mouse for that)
bindsym $mod+r mode "resize"
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        #bindsym j resize shrink width 10 px or 10 ppt
        #bindsym k resize grow height 10 px or 10 ppt
        #bindsym l resize shrink height 10 px or 10 ppt
        #bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
##bar {
#        status_command i3status
#	position top
#	colors {
#		background #111111
#		statusline #ffffff
#		separator #ffc5022
#	}
#}
# End_bindsym

# End_i3-config-file
